<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Silicon Valley Blog</title>
  <link rel="stylesheet" href="/public/css/styles.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" 
  integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
   <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
    integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf"
    crossorigin="anonymous"/>
</head>

<body>
    
    <div class="flex-column justify-space-around">
        <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="container-fluid">
                    <h1>
                        <p class="blog-title display-flex justify-space-between align-center p-2">
                            <i class="fas fa-blog"></i> <strong>Silicon Valley Blog</strong>
                        </p>
                    </h1>
                    
                    <form class="d-flex">
                        {{#if loggedIn}}
                        <button href="/" type="button" class=" btn nav-link active btn-success " aria-current="page"> Homepage</button>
                        <a href="/dashboard">Dashboard</a>
                        <button href="/login" id="logout" class="btn btn-outline-success" type="submit">Log out</button>
                        {{else}}
                        <button href="/" type="button" class=" btn nav-link active btn-success " aria-current="page"> Homepage</button>
                        <button href="/login" class="btn btn-outline-success" type="submit">Log In</button>
                        {{/if}}
                    </form>
                </div>
            </nav>
            {{!-- <nav class="nav">
                {{#if loggedIn}}
                <a href="/" class=" nav-link active homepage" aria-current="page">Homepage</a>
                <a href="/dashboard">Dashboard</a>
                <button id="logout" type="button" class="nav-link active ">Logout</button>
                {{else}}
                <button href="/" type="button" class=" btn nav-link active btn-success " aria-current="page"> Homepage</button>
                <button href="/login" type="button" class=" btn nav-link active btn-success" >Login</button>
                {{/if}}
            </nav> --}}
        </header>
        <main class="container container-fluid mt-5">
        {{{ body }}}
        </main>
        <footer>
      
        </footer>
    </div>


<!-- Render script for logged in users only -->
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}
</body>

</html>