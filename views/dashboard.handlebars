<div class="mb-5 pt-3">
    <div>
        <a href="/dashboard/new-post/" class="btn btn-is-primary ">Create Blog</a>
        
        <h2 class="title pb-4">Your Blog: </h2><hr>
{{#if userPost.length}}
    </div>

    {{#each userPost as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="btn btn-is-primary">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="text-center fw-bold fs-1">Please Create Something Interesting </h6>

{{/if}}


