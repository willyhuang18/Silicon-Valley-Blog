<div class="mb-5 pt-3">
    <div>
        <div>
    
        <h2 class="title pb-4">Your Blog: </h2>
        <a href="/dashboard/new-post/" type="button"class="btn btn-primary" > Create Blog </a>
        <hr>
        </div>
{{#if userPost.length}}
    </div>

    {{#each userPost as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button type="button" class="btn btn-primary">edit post</button></a>
</div>
    {{/each}}

{{else}}
<h6 class="text-center fw-bold fs-1">Please Create Something Interesting </h6>

{{/if}}


