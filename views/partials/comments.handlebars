<div class="card mb-6">
  {{#each this}}
  <section>
    <div>
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="card-body">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>